skill: claim-live-smoke
runners:
  default:
    default: true
    type: cli-tool
    command: node
    args:
      - -e
      - "process.stdout.write('runx claim live smoke')"
    timeout_seconds: 10
    inputs: {}
    runx:
      input_resolution:
        required: []
harness:
  cases: []
